# อัปเดต Supplier checkout v11

## ติดตั้งตามลำดับ
1. ใน Supabase SQL Editor รันไฟล์ `supabase-supplier-points-v11.sql` ทั้งไฟล์ก่อน ต้องติดตั้ง supplier checkout/runtime/main-store ชุดเดิมแล้ว
2. เมื่อ SQL สำเร็จ อัปโหลดไฟล์เว็บใน ZIP ทับตำแหน่งเดิมบน repo รวมโฟลเดอร์ `lib/suppliers` แล้ว Deploy Production
3. เปิดหน้าสินค้าใหม่ด้วย Ctrl+Shift+R ไม่ต้องเปลี่ยน Environment Variables

ไฟล์ SQL เป็น migration เพิ่มการใช้ Point ไม่ใช่ไฟล์สำหรับเรียกซื้อสินค้า ไม่มีการซื้อจริงหรือเติม/ปรับยอด Point เดิมขณะรัน migration ไม่มี API Function เพิ่ม

## สิ่งที่เปลี่ยน
- หน้ารอไม่ระบุชื่อผู้ให้บริการแล้ว และหน้า QR เหลือวงโหลดจาก CSS อันเดียว
- เปิดใช้ Point ของเว็บเดิม (`user_points` และ `point_transactions`) สำหรับสินค้าที่ดึงจาก API อัตรา 1 Point = 1 บาท
- Point ครบยอด: ตัดแต้มและสร้างออเดอร์ใน transaction เดียว จากนั้นเซิร์ฟเวอร์ดำเนินการจัดส่งผ่าน API ไม่ต้องโอนหรือแนบสลิป
- Point ไม่ครบ: ใช้ Point ตามที่เลือก และชำระยอดคงเหลือด้วยช่องทางเดิม ก่อนจัดส่งต้องมีหลักฐานการตัด Point และผลยืนยันยอดเงินส่วนที่เหลือ
- ตรวจยอดฝั่งเซิร์ฟเวอร์ ล็อกกระเป๋าขณะตัดแต้ม กันยอดติดลบและการส่งคำขอเดิมซ้ำ
- เมื่อออเดอร์ที่ยังไม่ชำระถูกเปลี่ยนเป็น cancelled/expired จะคืน Point ครั้งเดียว รองรับกรณีระบบยกเลิกเดิมคืนแต้มให้แล้ว ไม่ได้เพิ่มตัวตั้งเวลาหมดอายุใหม่
- ออเดอร์ที่จ่ายแล้วหรือเริ่มซื้อจากผู้ให้บริการแล้วไม่คืนแต้มอัตโนมัติ ต้องตรวจผลการจัดส่งก่อน เพื่อไม่คืนแต้มพร้อมส่งสินค้าไปด้วย
- คูปองยังปิดสำหรับสินค้า Supplier ตามเดิม

## ตรวจหลังติดตั้ง
ตรวจยอด Point ก่อน/หลังหนึ่งออเดอร์ และยอด QR ต้องเป็นราคาสินค้าลบ Point จริง เช่น ราคา 54 บาท ใช้ 20 Point เหลือ 34 บาท อย่ากดซื้อซ้ำหากคำขอหมดเวลา ให้ดูออเดอร์เดิมก่อน

การยืนยันซื้อด้วย Point เต็มยอดเป็นการสั่งซื้อจริง และอาจตัดเงินร้านที่ผู้ให้บริการเพื่อจัดส่งตามระบบเดิม ไม่ควรทดลองด้วยบัญชีจริงหากยังไม่ต้องการซื้อ

หากชำระด้วย Point แล้วแต่จัดส่งไม่สำเร็จ ให้เปิดออเดอร์ของฉันเพื่อตรวจสถานะ/รับบัญชี หรือติดต่อร้านด้วยเลขออเดอร์ อย่าจ่ายซ้ำ ไม่แนบ API Key หรือบัญชีที่ได้รับ

ชุดนี้ทดสอบฐานข้อมูลและ API ด้วยข้อมูลจำลอง รวมกรณี Point เต็ม/บางส่วน ยอดไม่พอ หลักฐานการจ่ายไม่ครบ คำขอซ้ำ การยกเลิก/หมดอายุ และสิทธิ์เข้าถึง พร้อมทดสอบหน้ารอที่ 390/768/1440px ยังไม่ได้ติดตั้ง SQL หรือ Deploy บน Production และไม่ได้ทดสอบซื้อเงินจริง
