# Supplier checkout loading v10

## ติดตั้ง
แตกไฟล์ host-upload-supplier-loading-v10.zip แล้วนำไฟล์ทั้งหมดทับที่ root ของ repo ร้าน รวมโฟลเดอร์ lib/suppliers ให้ตรงตำแหน่งเดิม จากนั้น Deploy Production บน Vercel แล้วรีเฟรชด้วย Ctrl+Shift+R

ไม่ต้องแก้ Environment Variables หรือรัน SQL เพิ่ม ไม่มี API Function ใหม่ และ ZIP ไม่มีไฟล์คีย์หรือ .env

## สิ่งที่แก้
- แสดงหน้ารอแบบเคลื่อนไหวระหว่างตรวจราคา/สต็อกและสร้างออเดอร์ รองรับ reduced motion
- ล็อกปุ่มซื้อระหว่างตรวจราคา และรวมคำขอตรวจราคาที่เกิดพร้อมกันสำหรับผู้ใช้/สินค้าเดียวกัน
- เมื่อเซิร์ฟเวอร์จำกัดคำขอ พักการตรวจราคาและสร้างออเดอร์ตาม Retry-After โดยยังเปิดดูออเดอร์เดิมได้ ไม่ส่งซื้อซ้ำอัตโนมัติ
- แยก Supabase HTTP 429 เป็น SUPPLIER_DATABASE_RATE_LIMITED โดยไม่เปิดเผยข้อความภายในฐานข้อมูล
- รูป QR ไม่ใช้ระบบภาพสำรองของสินค้า ป้องกันภาพตกแต่งมาแทน QR
- หากรูป QR ไม่โหลดภายในเวลาที่กำหนด ให้ลอง URL สำรองที่มีอยู่ และแสดงปุ่มโหลด QR ออเดอร์เดิมอีกครั้ง ไม่สร้างออเดอร์ใหม่

## ข้อวินิจฉัยและข้อจำกัด
ภาพ SUPPLIER_RATE_LIMITED ไม่ใช่หลักฐานว่า Supabase เต็ม ในโค้ดร้านการตรวจราคาและสร้างออเดอร์ใช้ลิมิตร่วมกัน 3 คำขอ/นาที/ผู้ใช้ การกดหลายครั้งจึงชนลิมิตได้ ชุดนี้คงลิมิตฝั่งเซิร์ฟเวอร์และการตรวจราคาจริงก่อนซื้อไว้ ไม่หลบข้อจำกัดของ 499K

ยังไม่ได้ยืนยันสาเหตุ QR ล้มเหลวบน Production จาก network log ชุดนี้แก้การจัดการข้อผิดพลาดและการโหลดใหม่เท่านั้น ไม่สามารถแก้ช่องทางรับเงินที่ตั้งค่าผิดหรือบริการรูป QR ภายนอกล่มได้

## ตรวจหลัง Deploy
1. กดซื้อสินค้าจาก API หนึ่งครั้ง ต้องเห็นหน้ารอ แล้วเงื่อนไขสั่งซื้อเดิม
2. หากถูกจำกัดคำขอ ให้รอเวลาที่แจ้ง ไม่กดซื้อซ้ำหรือ Import ซ้ำ
3. หลังยืนยันออเดอร์ ต้องแสดงเลขออเดอร์และยอดที่ตรงกันในหน้า QR
4. ถ้ารูป QR ไม่ขึ้น ให้ใช้ปุ่มโหลด QR ออเดอร์เดิม ไม่เปิดออเดอร์ใหม่ ตรวจชื่อผู้รับและยอดก่อนโอน
5. ถ้ายังผิดพลาด ส่งเฉพาะรหัสข้อผิดพลาด/เวลา/สถานะ HTTP ห้ามส่ง API Key, token, service-role key หรือข้อมูลบัญชีที่จัดส่ง

ทดสอบด้วยข้อมูลจำลอง: supplier unit/integration 144 รายการผ่าน; browser checkout fixture ผ่าน; หน้ารอที่ 390/768/1440px และ reduced motion ผ่าน ไม่มีการซื้อ/โอนเงินจริงในการทดสอบ และยังไม่ได้ Deploy ให้
